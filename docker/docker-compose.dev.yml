services:
  docs-dev:
    image: node:24-alpine
    working_dir: /app
    command: sh -c "corepack enable && pnpm install && pnpm start --host 0.0.0.0"
    ports:
      - "3001:3000"
    volumes:
      - ..:/app
      - /app/node_modules
